{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "vite --host",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "git checkout build && git rebase - && npm run build",
    "deploy": "git add . && git diff --quiet && git diff --staged --quiet || git commit -m \"build: $(date)\" ",
    "postdeploy": "git push origin build -f && git checkout -"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/marked": "^3.0.1",
    "sass": "^1.42.1",
    "typescript": "^4.4.3",
    "vite": "^2.5.7",
    "vite-plugin-solid": "^2.0.3"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.4",
    "github-markdown-css": "^4.0.0",
    "highlight.js": "^11.2.0",
    "marked": "^3.0.7",
    "solid-js": "^1.1.3"
  }
}
